# Historical Release Artifacts

Snapshots antigos podem permanecer aqui apenas como referência/offline backup.

Eles **não são usados para executar o STEH atual**.

A baseline corrente está na raiz do repositório.

Depois que o repositório estiver no GitHub, as tags e GitHub Releases devem se tornar a fonte oficial do histórico.

Historical snapshot included: `v0.2.1-alpha.zip`.
